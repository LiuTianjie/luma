[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "lae-luma-adapter"
version = "0.1.0"
description = "Typed, tenant-safe LAE adapter for Luma Builder Task v1"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
