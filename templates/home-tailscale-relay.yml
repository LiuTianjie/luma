services:
  app:
    image: ghcr.io/your-org/home-panel:latest
    ports:
      - target: 8080
        published: 8080
        protocol: tcp
        mode: host
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.region == home
