version: "3.8"

services:
  worker:
    image: ghcr.io/your-org/your-worker:latest
    environment:
      QUEUE_URL: redis://redis:6379/0
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.labels.region == global
