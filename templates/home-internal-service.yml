# Luma manifest template — home internal service (no public ingress).
# Deploy with: luma deploy home-internal-service.yml
name: internal-tool
image: ghcr.io/your-org/internal-tool:latest
region: home
exposure: none
replicas: 1
